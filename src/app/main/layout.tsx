// import AppLayout from "@/components/Layout";

export default function MainLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      <div>12345</div>
      {children}
    </div>
  );
}
